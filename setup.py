from distutils.core import setup

setup(name='santa-helper',
      version='0.1',
      description='Python Santa Helper',
      author='Ricardo Xavier',
      requires=[],
      packages=['distutils', 'distutils.command'],
     )
